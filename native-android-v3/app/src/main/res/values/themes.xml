<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pre-Compose window theme only. Everything visual lives in AiiminTheme;
         this exists so the launch window is already the Drafting Table ground. -->
    <style name="Theme.Aiimin.Starting" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/drafting_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
