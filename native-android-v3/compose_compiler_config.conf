// Types the Compose compiler should treat as stable even though it cannot prove
// it — domain models are immutable by construction. Add packages as the core
// model module grows.
aiimin.designsystem.theme.*
