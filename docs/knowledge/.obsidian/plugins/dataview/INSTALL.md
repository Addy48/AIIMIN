# Plugin install note

Binaries (`main.js`, `manifest.json`, `styles.css`) are vendored in this folder for vault portability.

**Founder:** Obsidian → Settings → Community plugins → ensure plugin enabled after first trust prompt.

Denylist paths never targeted by capture macros: `Genesis/**`, `Roadmap/UX-Architecture/**`, `Roadmap/UX-Intelligence/**`.
