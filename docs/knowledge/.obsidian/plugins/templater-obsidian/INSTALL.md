# Plugin install note

Binaries vendored. Enable in Community plugins after trust.

Templates folder: `_templates`. Folder templates configured in `data.json`.
System commands: **off**. Never write Genesis / UXA / UXI.
