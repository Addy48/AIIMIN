# Recovery Branch Inventory — 2026-07-08

Branch: `recovery/pre-waitlist-full` (4 commits ahead of `main` at recovery start, plus stabilization commit).

## What this branch recovers

Pre-waitlist dashboard work that was lost or reverted during the waitlist pivot (Jul 4–7 sessions). Merged with current waitlist/auth stack from `main` (c708c181).

| Area | Status | Notes |
|------|--------|-------|
| Design Lab | ✅ Recovered | Account → Design section, prototypes, honest archive labels |
| 2-theme system | ⚠️ Partial | `aiimin-dark` / `aiimin-light` in ThemeContext; legacy nordic/vercel themes still in CSS |
| Masthead navbar | ⚠️ Partial | Recovered in Navbar.jsx; some prototype tabs still differ from live |
| Trajectory sun-arc | ✅ Shipped | Overview sidebar execution widget |
| Journal Studio | ✅ Recovered | Journal page + studio wiring |
| Finance ShippedSubNav | ✅ Shipped | Wealth crash fixed |
| Widget grid / personalization | ⚠️ Partial | Overview decentralization wiring; full persona presets incomplete |
| Waitlist + auth routing | ✅ Wired | `App.js` routes, `useAccessGate`, login/signup/onboarding compatible |
| Production env | ✅ Committed | `frontend/.env.production` (public keys only) + build verifier |

## Commits on branch (before stabilization)

1. `decf6991` — Design Lab, 2-theme system, widget grid
2. `190e2df5` — Masthead nav, trajectory sun-arc, lab wiring
3. `89cc9c9e` — Journal Studio, Finance ShippedSubNav
4. `fba9bcfb` — Finance Wealth crash fix, dashboard UX

## Stabilization (this session)

- Removed auth debug instrumentation (`127.0.0.1:7876` fetch logs)
- Honest Design Lab archive status labels (Partial / Prototype, not false "Shipped")
- `frontend/.env.production` committed for Vercel parity
- `verify-production-env.mjs` wired into root `npm run build`
- `npm run build` passes locally

## How to run

### Local full dashboard (recommended for dev)

```bash
cp frontend/.env.example frontend/.env.local
# Set REACT_APP_WAITLIST_MODE=false in .env.local
# Fill REACT_APP_SUPABASE_URL + REACT_APP_SUPABASE_ANON_KEY from Supabase dashboard
npm run dev
```

Visit `http://localhost:3000` → login redirect → full dashboard.

### Local waitlist mode (test production UX)

```bash
# In frontend/.env.local:
REACT_APP_WAITLIST_MODE=true
REACT_APP_TESTER_EMAILS=your-tester@gmail.com
```

`/` shows waitlist landing; testers bypass via allowlist.

### Production (Vercel)

- `frontend/.env.production` has `REACT_APP_WAITLIST_MODE=true`
- Vercel dashboard env should match (or override) committed file
- Testers: 6 emails in `REACT_APP_TESTER_EMAILS` + server `tester_allowlist`

## Route map (App.js)

| Path | Waitlist mode | Full mode |
|------|---------------|-----------|
| `/` | Waitlist landing (public) | Redirect → login or overview |
| `/login` | Login + waitlist CTA | Login |
| `/auth/callback` | OAuth handler | OAuth handler |
| `/onboarding` | Blocked for non-testers | Profile setup |
| `/account?section=design` | Design Lab (testers) | Design Lab |
| `/design-lab` | Redirect → account design | Redirect → account design |
| `/overview` etc. | Testers only | Authenticated users |

## Excluded from git (intentional)

| Path | Reason |
|------|--------|
| `.env`, `.env.local`, `Secrets, Keys /` | Secrets |
| `.agents/`, `.qwen/` | Agent skill caches |
| `.cursor/debug-*.log` | Debug session logs |
| `skills-lock.json` | Local skill registry noise |
| `frontend/build/` | Build output |

## Known issues (honest)

1. **LC-12 E2E** — Manual tester login → onboarding → overview not re-verified after recovery merge
2. **GA4 + Sentry** — Env vars not set (`REACT_APP_GA_MEASUREMENT_ID`, `REACT_APP_SENTRY_DSN`)
3. **Typography** — Figtree/Bodoni planned; Inter/Playfair still loaded globally
4. **Stripe billing** — No Stripe SDK crash; subscription UI uses waitlist pricing copy, live billing deferred
5. **Theme parity** — Switching to aiimin-dark/light may leave some legacy green accents on old token blocks
6. **Resend API key** — Rotate if exposed in prior chat sessions

## Merge recommendation

Build passes. Safe to merge `recovery/pre-waitlist-full` → `main` and push. Vercel auto-deploys from `main`.

*Generated 2026-07-08*
