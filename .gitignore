node_modules/
.DS_Store

# Local secrets folder (screenshots, keys — never commit)
Secrets, Keys /
Secrets, Keys/**

# Vercel CLI pull (may contain stale workers.dev URLs + tokens)
.env.vercel

# Environment files (never commit secrets)
.env
.env.local
.env.production.local
.env.development.local
.env.test.local
frontend/.env
frontend/.env.local
frontend/.env.production.local
backend/.env
backend/.env.local
backend/.env.production

# Build outputs
frontend/build/

# Capacitor / Android local secrets
frontend/android/local.properties
frontend/android/.gradle/
frontend/android/app/build/
frontend/android/build/
frontend/android/capacitor-cordova-android-plugins/build/

# SSH keys (never commit)
*.pem
client_secret*.json
aiimin.pem

# Vercel
.vercel

# MCP local config (may contain secrets — use .mcp.json.example)
.mcp.json
mcp.json

# EC2 paste file with production secrets — never commit
deploy/EC2.env.paste

# Account Design Lab — tracked in repo (was local-only prototypes)

# Tools & editors (local only — never ship on GitHub)
.claude/
.cursor/
.cursor/**
.qwen/
.qwen/**
.superpowers/
.sst/
.sst

# Local QA dumps / agent noise (never ship)
**/debug-*.log
docs/knowledge/12_SPRINTS/protocol-shots/

# One-time scripts (safe to delete after use)
create_user.js
setup_db_script.js

# Render (not in use)
render.yaml
.env*
!.env.example
!**/.env.example
!frontend/.env.production
!frontend/.env.development
!**/.env.production.example
