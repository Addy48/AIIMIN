node_modules/
.DS_Store

# Local secrets folder (screenshots, keys — never commit)
Secrets, Keys /
Secrets, Keys/**

# Vercel CLI pull (may contain stale workers.dev URLs + tokens)
.env.vercel

# Environment files (never commit secrets)
.env
.env.local
.env.production
.env.production.local
.env.development.local
.env.test.local
frontend/.env
frontend/.env.local
frontend/.env.production
frontend/.env.production.local
backend/.env
backend/.env.local
backend/.env.production

# Build outputs
frontend/build/

# SSH keys (never commit)
*.pem
client_secret*.json
aiimin.pem

# Vercel
.vercel

# MCP local config (may contain secrets — use .mcp.json.example)
.mcp.json

# Account Design Lab — local prototypes only (not for production)
frontend/src/pages/account/sections/DesignSection.jsx
frontend/src/pages/account/sections/design/

# Tools & editors
.claude/
.sst/
.sst

# One-time scripts (safe to delete after use)
create_user.js
setup_db_script.js

# Render (not in use)
render.yaml
.env*
!.env.example
!**/.env.example
!**/.env.production.example
